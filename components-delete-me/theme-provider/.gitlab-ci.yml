test_theme-provider:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi-digital/components/theme-provider"
    ROOT_DIR: "components/theme-provider"

build_theme-provider:
  extends: .build
  variables:
    WORKSPACE: "@bmi-digital/components/theme-provider"
    ROOT_DIR: "components/theme-provider"
