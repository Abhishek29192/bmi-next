test_container:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi-digital/components/container"
    ROOT_DIR: "components/container"

build_container:
  extends: .build
  variables:
    WORKSPACE: "@bmi-digital/components/container"
    ROOT_DIR: "components/container"
