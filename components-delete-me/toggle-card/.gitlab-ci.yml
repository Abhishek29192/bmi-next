test_toggle-card:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi-digital/components/toggle-card"
    ROOT_DIR: "components/toggle-card"

build_toggle-card:
  extends: .build
  variables:
    WORKSPACE: "@bmi-digital/components/toggle-card"
    ROOT_DIR: "components/toggle-card"
