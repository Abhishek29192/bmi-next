test_filters:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi-digital/components/filters"
    ROOT_DIR: "components/filters"

build_filters:
  extends: .build
  variables:
    WORKSPACE: "@bmi-digital/components/filters"
    ROOT_DIR: "components/filters"
