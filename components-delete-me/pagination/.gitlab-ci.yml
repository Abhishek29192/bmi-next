test_pagination:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi-digital/components/pagination"
    ROOT_DIR: "components/pagination"

build_pagination:
  extends: .build
  variables:
    WORKSPACE: "@bmi-digital/components/pagination"
    ROOT_DIR: "components/pagination"
