build_redirects_website_hr:
  extends: .build_yarn_workspace
  rules:
    - !reference [.ingore_release_mrs, rules]
    - !reference [.run_for_changes_on_mr, rules]
    - !reference [.run_for_changes_on_main, rules]
  variables:
    ROOT_DIR: applications/redirects-website-hr
    WORKSPACE: "@bmi/redirects-website-hr"
