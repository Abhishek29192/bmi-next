lint_redirects_website_uk_redland:
  extends: .lint_yarn_workspace
  rules:
    - !reference [ .ignore_release_mrs, rules ]
    - !reference [ .run_for_changes_on_main, rules ]
    - !reference [ .run_for_changes_on_mr, rules ]
  variables:
    ROOT_DIR: "applications/dxb/redirects-website-uk-redland"
    WORKSPACE: "@bmi/redirects-website-uk-redland"

build_redirects_website_uk_redland:
  extends: .build_yarn_workspace
  rules:
    - !reference [ .ignore_release_mrs, rules]
    - !reference [.run_for_changes_on_mr, rules]
    - !reference [.run_for_changes_on_main, rules]
  variables:
    ROOT_DIR: applications/redirects-website-uk-redland
    WORKSPACE: "@bmi/redirects-website-uk-redland"
