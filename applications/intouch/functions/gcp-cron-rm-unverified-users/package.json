{
  "name": "@bmi/gcp-cron-rm-unverified-users",
  "version": "0.0.0",
  "main": "dist/index.js",
  "description": "BMI InTouch Function remove auth0 user",
  "license": "BMI-DXB",
  "private": true,
  "scripts": {
    "build": "webpack --mode=production",
    "compile": "tsc",
    "dev": "yarn build && js-yaml ./.env.yaml > ./.env.json && env-cmd -r ./.env.json functions-framework --source=dist --target=removeAuth0UnverifiedAccount --port=3001",
    "lint": "eslint \"**/*.{js,ts,tsx}\"",
    "test": "jest --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "@bmi-digital/functions-logger": "1.2.0",
    "auth0": "^2.40.0",
    "date-fns": "^2.28.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.0.0",
    "env-cmd": "^10.1.0",
    "js-yaml": "^4.0.0",
    "webpack": "^5.70.0",
    "webpack-cli": "^4.9.2"
  }
}
