build_intouch_migration_db_service:
  stage: build
  image: google/cloud-sdk
  services:
    - docker:dind
  cache: {}
  rules:
    - if: "$CI_PIPELINE_SOURCE == 'merge_request_event' || ($CI_PIPELINE_SOURCE == 'push' && $CI_COMMIT_BRANCH == 'master') || ($CI_PIPELINE_SOURCE == 'push' && $CI_COMMIT_BRANCH == 'intouch/onboarding')"
      changes:
        - applications/intouch/api/db-migrations/**/*
        - applications/intouch/api/services/companies/src/data/**/*
  script:
    - echo $GCP_INTOUCH_REPLATFORM_SERVICE_KEY > gcloud-service-key.json # Google Cloud service accounts
    - gcloud auth activate-service-account --key-file gcloud-service-key.json
    - gcloud config set project $GCP_INTOUCH_REPLATFORM_PROJECT_ID
    - gcloud builds submit . --config=applications/intouch/api/db-migrations/cloudbuild.migration.build.yaml --substitutions _CI_COMMIT_SHORT_SHA=$CI_COMMIT_SHORT_SHA

deploy_intouch_migration_db_service:
  stage: migration
  needs:
    - build_intouch_migration_db_service
  image: google/cloud-sdk
  services:
    - docker:dind
  cache: {}
  rules:
    - if: "$CI_PIPELINE_SOURCE == 'push' && $CI_COMMIT_BRANCH == 'intouch/onboarding'"
      changes:
        - applications/intouch/api/db-migrations/**/*
        - applications/intouch/api/services/companies/src/data/**/*
  script:
    - echo $GCP_INTOUCH_REPLATFORM_SERVICE_KEY > gcloud-service-key.json # Google Cloud service accounts
    - gcloud auth activate-service-account --key-file gcloud-service-key.json
    - gcloud config set project $GCP_INTOUCH_REPLATFORM_PROJECT_ID
    - gcloud builds submit . --config=applications/intouch/api/db-migrations/cloudbuild.migration.deploy.yaml --substitutions _CI_COMMIT_SHORT_SHA=$CI_COMMIT_SHORT_SHA
# migrate_company_db:
#   stage: migration
#   needs:
#     - install
#     - job: deploy_intouch_migration_db_service
#       optional: true
#   variables:
#     MIGRATION_SERVICE_URL: https://tf-db-migrations-rfwslk3zjq-nw.a.run.app
#   rules:
#     - if: "$CI_PIPELINE_SOURCE == 'push' && $CI_COMMIT_BRANCH == 'intouch/onboarding'"
#       changes:
#         - applications/intouch/api/services/companies/src/data/*
#   before_script:
#     - yarn config set cache-folder ${PWD}/.yarn_cache
#   script:
#     - yarn workspace @bmi/intouch-api-migration-scripts migrate:companies

# migrate_training_db:
#   stage: migration
#   needs:
#     - install
#     - job: deploy_intouch_migration_db_service
#       optional: true
#   variables:
#     MIGRATION_SERVICE_URL: https://tf-db-migrations-rfwslk3zjq-nw.a.run.app
#   rules:
#     - if: "$CI_PIPELINE_SOURCE == 'push' && $CI_COMMIT_BRANCH == 'intouch/onboarding'"
#       changes:
#         - applications/intouch/api/services/training/src/data/*
#   before_script:
#     - yarn config set cache-folder ${PWD}/.yarn_cache
#   script:
#     - yarn workspace @bmi/intouch-api-migration-scripts migrate:training
