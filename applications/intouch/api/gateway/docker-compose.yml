version: "3"
services:
  # The application image
  graph-gateway-service:
    build:
      context: .
      dockerfile: dockerfile
    environment:
      ENV_NAME: dev
      COMPANY_SERVICE: https://companies-akoergu63a-nw.a.run.app/graphql
      PRODUCT_SERVICE: https://products-akoergu63a-nw.a.run.app/graphql
    ports:
      - 4000:4000
