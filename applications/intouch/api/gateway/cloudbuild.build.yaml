steps:
  # build the container image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        ".",
        "-t",
        "gcr.io/$PROJECT_ID/intouch-graph-gateway:${_CI_COMMIT_SHORT_SHA}",
        "-f",
        "./applications/intouch/api/gateway/dockerfile",
        "--build-arg",
        "NPM_AUTH_READ_TOKEN=${_NPM_AUTH_READ_TOKEN}"
      ]

  # push the container image
  - name: "gcr.io/cloud-builders/docker"
    args:
      ["push", "gcr.io/$PROJECT_ID/intouch-graph-gateway:${_CI_COMMIT_SHORT_SHA}"]

timeout: 1200s

logsBucket: "gs://intouch-replaform-bucket"
