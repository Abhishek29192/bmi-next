overwrite: true
schema:
  - http://localhost:4000/graphql:

hooks:
  afterAllFileWrite:
    - prettier --write
generates:
  ./generated/types.ts:
    plugins:
      - typescript

  ./generated/schema.graphql:
    plugins:
      - schema-ast
