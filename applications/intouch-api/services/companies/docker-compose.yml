version: "3"
services:
  # The application image
  companies-service:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      ENV_NAME: dev
      DATABASE: postgres
      PG_USER: postgres
      PASSWORD: bHpuecIrItH6wEJ4
      HOST: 35.246.58.59
      PG_PORT: 5432
      PG_SCHEMA: public
    ports:
      - 4001:4001
