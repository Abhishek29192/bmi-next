import { styled } from "@mui/material/styles";

export const StyledLinkDialog = styled("div")(({ theme }) => ({
  marginBottom: "2rem",
  maxHeight: "100%",
  overflow: "auto",
  padding: "0 2rem"
}));
