import { styled } from "@mui/material/styles";

export const BackToResultsElement = styled("div")(() => ({
  display: "flex"
}));

export const BackToResultsSeparator = styled("div")(() => ({
  marginLeft: "8px",
  marginRight: "8px",
  width: "1px",
  justifySelf: "stretch",
  backgroundColor: "#cccccc"
}));
