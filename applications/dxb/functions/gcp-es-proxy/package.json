{
  "name": "@bmi/gcp-es-proxy",
  "private": true,
  "description": "Simple NodeJS proxy to query Elastic Search",
  "version": "0.0.0",
  "keywords": [
    "node",
    "elastic search"
  ],
  "license": "BMI-DXB",
  "scripts": {
    "build": "tsc",
    "dev": "yarn build && js-yaml ./.env.yaml > ./.env.json && env-cmd -r ./.env.json functions-framework --source=dist --target=proxy --port=9052",
    "deploy": "yarn build && gcloud functions deploy es-proxy --trigger-http --runtime=nodejs14 --allow-unauthenticated --max-instances=5 --region=europe-west3 --entry-point=proxy --env-vars-file=.env.yaml",
    "coverage:jest": "jest --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^1.7.1",
    "node-fetch": "^2.6.1",
    "uuid": "^8.3.2"
  }
}
