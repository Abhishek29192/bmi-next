{
  "name": "@bmi/gcp-pim-message-handler",
  "version": "0.0.0",
  "license": "BMI-DXB",
  "main": "dist/index.js",
  "scripts": {
    "build": "webpack --mode=production",
    "check:ts": "tsc",
    "coverage:jest": "jest --coverage --reporters=default --reporters=jest-junit",
    "dev": "yarn build && js-yaml ./.env.yaml > ./.env.json && env-cmd -r ./.env.json functions-framework --source=dist --target=handleRequest --port=9056"
  },
  "dependencies": {
    "@bmi/functions-logger": "0.0.0",
    "@bmi/pim-api": "0.0.0",
    "@google-cloud/functions-framework": "^1.7.1",
    "@google-cloud/pubsub": "^2.5.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "js-yaml": "^4.0.0"
  }
}
