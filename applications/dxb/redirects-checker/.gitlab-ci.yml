build_redirects-checker:
  extends: .build_yarn_workspace
  rules:
    - !reference [ .ingore_release_mrs, rules ]
    - !reference [ .run_for_changes_on_mr, rules ]
  variables:
    WORKSPACE: "@bmi/redirects-checker"
    ROOT_DIR: "applications/dxb/redirects-checker"
