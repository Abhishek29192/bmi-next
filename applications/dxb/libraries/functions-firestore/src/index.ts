import type {
  DocumentData,
  Firestore,
  Query,
  DocumentReference,
  CollectionReference
} from "firebase-admin/firestore";
import { getFirestore } from "./firestore";

export {
  getFirestore,
  DocumentData,
  Firestore,
  Query,
  DocumentReference,
  CollectionReference
};
