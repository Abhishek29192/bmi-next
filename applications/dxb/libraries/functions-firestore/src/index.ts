import { getFirestore } from "./firestore";
import type {
  DocumentData,
  Firestore,
  Query,
  DocumentReference,
  CollectionReference
} from "firebase-admin/firestore";

export {
  getFirestore,
  DocumentData,
  Firestore,
  Query,
  DocumentReference,
  CollectionReference
};
