lint_functions-firestore:
  extends: .lint_yarn_workspace
  rules:
    - !reference [ .ignore_release_mrs, rules ]
    - !reference [ .run_for_changes_on_main, rules ]
    - !reference [ .run_for_changes_on_mr, rules ]
  variables:
    ROOT_DIR: "applications/dxb/libraries/functions-firestore"
    WORKSPACE: "@bmi/functions-firestore"

build_functions-firestore:
  extends: .build_yarn_workspace
  rules:
    - !reference [ .ignore_release_mrs, rules ]
    - !reference [ .run_for_changes_on_main, rules ]
    - !reference [ .run_for_changes_on_mr, rules ]
  variables:
    WORKSPACE: "@bmi/functions-firestore"
    ROOT_DIR: "applications/dxb/libraries/functions-firestore"

test_functions-firestore:
  extends: .test_yarn_workspace
  rules:
    - !reference [ .ignore_release_mrs, rules ]
    - !reference [ .run_for_changes_on_main, rules ]
    - !reference [ .run_for_changes_on_mr, rules ]
  variables:
    WORKSPACE: "@bmi/functions-firestore"
    ROOT_DIR: "applications/dxb/libraries/functions-firestore"
  needs:
    - build_functions-firestore
