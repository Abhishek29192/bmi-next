image: node:12

build_gcp-download-zip:
  stage: build
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_MERGE_REQUEST_ID'
      changes:
        - functions/gcp-download-zip/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-download-zip run build
  artifacts:
    paths:
      - functions/gcp-download-zip/dist
  interruptible: true

test_cp-download-zip:
  stage: test
  rules:
    - if: "$CI_COMMIT_TAG || $CI_MERGE_REQUEST_ID || $CI_COMMIT_BRANCH == 'master'"
      changes:
        - functions/gcp-download-zip/**
        - jest/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-download-zip coverage:jest
  interruptible: true

package_gcp-download-zip:
  stage: package
  rules:
    - if: '$CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "pre-production" || $CI_COMMIT_BRANCH == "production"'
      changes:
        - functions/gcp-download-zip/**
  needs: [build_gcp-download-zip]
  dependencies:
    - build_gcp-download-zip
  before_script:
    - apt-get update
    - apt-get install -y zip unzip
    - mkdir -p dist/functions
  script:
    - cd functions/gcp-download-zip; zip -r $CI_PROJECT_DIR/dist/functions/gcp-download-zip.zip package.json dist/; cd $CI_PROJECT_DIR;
  artifacts:
    paths:
      - $CI_PROJECT_DIR/dist/functions
  interruptible: true

# Deploy to QA Storage
deploy_qa_gcp-download-zip:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes:
        - functions/gcp-download-zip/**
      allow_failure: true
  needs: [package_gcp-download-zip]
  dependencies:
    - package_gcp-download-zip
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_QA"
    - gcloud config set project $GCP_DXB_PROJECT_ID_QA
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-download-zip.zip gs://$GCP_DXB_BUILD_STORAGE_QA/sources/

# Deploy to Pre-Production Storage
deploy_preproduction_gcp-download-zip:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "pre-production"'
      changes:
        - functions/gcp-download-zip/**
  needs: [package_gcp-download-zip]
  dependencies:
    - package_gcp-download-zip
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PREPROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PREPROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-download-zip.zip gs://$GCP_DXB_BUILD_STORAGE_PREPROD/sources/

# Deploy to Production Storage
deploy_production_gcp-download-zip:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "production"'
      changes:
        - functions/gcp-download-zip/**
  needs: [package_gcp-download-zip]
  dependencies:
    - package_gcp-download-zip
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-download-zip.zip gs://$GCP_DXB_BUILD_STORAGE_PROD/sources/
