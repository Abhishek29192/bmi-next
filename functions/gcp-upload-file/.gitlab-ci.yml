image: node:12

test_gcp-upload-file:
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_MERGE_REQUEST_ID'
      changes:
        - functions/gcp-upload-file/**
        - jest/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-upload-file coverage:jest
  interruptible: true

build_gcp-upload-file:
  stage: build
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_MERGE_REQUEST_ID'
      changes:
        - functions/gcp-upload-file/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-upload-file run build
  artifacts:
    paths:
      - functions/gcp-upload-file/dist
  interruptible: true

package_gcp-upload-file:
  stage: package
  rules:
    - if: '$CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "pre-production" || $CI_COMMIT_BRANCH == "production"'
      changes:
        - functions/gcp-upload-file/**
  needs: [build_gcp-upload-file]
  dependencies:
    - build_gcp-upload-file
  before_script:
    - apt-get update
    - apt-get install -y zip unzip
    - mkdir -p dist/functions
  script:
    - cd functions/gcp-upload-file; zip -r $CI_PROJECT_DIR/dist/functions/gcp-upload-file.zip package.json dist/; cd $CI_PROJECT_DIR;
  artifacts:
    paths:
      - $CI_PROJECT_DIR/dist/functions
  interruptible: true

# Deploy to QA Storage
deploy_qa_gcp-upload-file:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes:
        - functions/gcp-upload-file/**
      allow_failure: true
  needs: [package_gcp-upload-file]
  dependencies:
    - package_gcp-upload-file
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_QA"
    - gcloud config set project $GCP_DXB_PROJECT_ID_QA
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-upload-file.zip gs://$GCP_DXB_BUILD_STORAGE_QA/sources/

# Deploy to Pre-Production Storage
deploy_preproduction_gcp-upload-file:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "pre-production"'
      changes:
        - functions/gcp-upload-file/**
  needs: [package_gcp-upload-file]
  dependencies:
    - package_gcp-upload-file
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PREPROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PREPROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-upload-file.zip gs://$GCP_DXB_BUILD_STORAGE_PREPROD/sources/

# Deploy to Production Storage
deploy_production_gcp-upload-file:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "production"'
      changes:
        - functions/gcp-upload-file/**
  needs: [package_gcp-upload-file]
  dependencies:
    - package_gcp-upload-file
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-upload-file.zip gs://$GCP_DXB_BUILD_STORAGE_PROD/sources/
