image: node:12

test_gcp-apsis-integration:
  stage: test
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_MERGE_REQUEST_ID'
      changes:
        - functions/gcp-apsis-integration/**
        - jest/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-apsis-integration coverage:jest
  interruptible: true

build_gcp-apsis-integration:
  stage: build
  rules:
    - if: '$CI_PIPELINE_SOURCE == "push" || $CI_MERGE_REQUEST_ID'
      changes:
        - functions/gcp-apsis-integration/**
  needs: [install]
  before_script:
    - yarn config set cache-folder ~/.yarn
  script:
    - yarn workspace @bmi/gcp-apsis-integration run build
  artifacts:
    paths:
      - functions/gcp-apsis-integration/dist
  interruptible: true

package_gcp-apsis-integration:
  stage: package
  rules:
    - if: '$CI_COMMIT_BRANCH == "master" || $CI_COMMIT_BRANCH == "pre-production" || $CI_COMMIT_BRANCH == "production" || $CI_COMMIT_TAG'
      changes:
        - functions/gcp-apsis-integration/**
  needs: [build_gcp-apsis-integration]
  dependencies:
    - build_gcp-apsis-integration
  before_script:
    - apt-get update
    - apt-get install -y zip unzip
    - mkdir -p dist/functions
  script:
    - cd functions/gcp-apsis-integration; zip -r $CI_PROJECT_DIR/dist/functions/gcp-apsis-integration.zip package.json dist/; cd $CI_PROJECT_DIR;
  artifacts:
    paths:
      - $CI_PROJECT_DIR/dist/functions
  interruptible: true

# Deploy to QA Storage
deploy_qa_gcp-apsis-integration:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      changes:
        - functions/gcp-apsis-integration/**
      allow_failure: true
  needs: [package_gcp-apsis-integration]
  dependencies:
    - package_gcp-apsis-integration
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_QA"
    - gcloud config set project $GCP_DXB_PROJECT_ID_QA
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-apsis-integration.zip gs://$GCP_DXB_BUILD_STORAGE_QA/sources/

# Deploy to Pre-Production Storage
deploy_preproduction_gcp-apsis-integration:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "pre-production"'
      changes:
        - functions/gcp-apsis-integration/**
  needs: [package_gcp-apsis-integration]
  dependencies:
    - package_gcp-apsis-integration
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PREPROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PREPROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-apsis-integration.zip gs://$GCP_DXB_BUILD_STORAGE_PREPROD/sources/

# Deploy to Production Storage
deploy_production_gcp-apsis-integration:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "production"'
      changes:
        - functions/gcp-apsis-integration/**
  needs: [package_gcp-apsis-integration]
  dependencies:
    - package_gcp-apsis-integration
  before_script:
    - gcloud auth activate-service-account --key-file="$GCP_DXB_SERVICE_KEY_PROD"
    - gcloud config set project $GCP_DXB_PROJECT_ID_PROD
  script:
    - gsutil cp $CI_PROJECT_DIR/dist/functions/gcp-apsis-integration.zip gs://$GCP_DXB_BUILD_STORAGE_PROD/sources/
