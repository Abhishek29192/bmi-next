{
  "name": "@bmi/gcp-firestore-writer",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && functions-framework --source=dist --target=handleMessage --port=3001"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "firebase-admin": "^9.2.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^1.7.1"
  }
}
