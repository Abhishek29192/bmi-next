test_header:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/header"
    ROOT_DIR: "components/header"

build_header:
  extends: .build
  variables:
    WORKSPACE: "@bmi/header"
    ROOT_DIR: "components/header"
