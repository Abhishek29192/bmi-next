test_promo-section:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/promo-section"
    ROOT_DIR: "components/promo-section"

build_promo-section:
  extends: .build
  variables:
    WORKSPACE: "@bmi/promo-section"
    ROOT_DIR: "components/promo-section"
