test_tabs:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/tabs"
    ROOT_DIR: "components/tabs"

test_mr_coverage_tabs:
  extends: .test_jest_mr_coverage
  variables:
    WORKSPACE: "@bmi/tabs"
    ROOT_DIR: "components/tabs"

build_tabs:
  extends: .build
  variables:
    WORKSPACE: "@bmi/tabs"
    ROOT_DIR: "components/tabs"
