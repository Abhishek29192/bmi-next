test_radio-group:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/radio-group"
    ROOT_DIR: "components/radio-group"

test_mr_coverage_radio-group:
  extends: .test_jest_mr_coverage
  variables:
    WORKSPACE: "@bmi/radio-group"
    ROOT_DIR: "components/radio-group"

build_radio-group:
  extends: .build
  variables:
    WORKSPACE: "@bmi/radio-group"
    ROOT_DIR: "components/radio-group"
