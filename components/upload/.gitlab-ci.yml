test_upload:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/upload"
    ROOT_DIR: "components/upload"

build_upload:
  extends: .build
  variables:
    WORKSPACE: "@bmi/upload"
    ROOT_DIR: "components/upload"
