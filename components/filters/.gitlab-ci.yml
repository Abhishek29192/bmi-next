test_filters:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/filters"
    ROOT_DIR: "components/filters"

build_filters:
  extends: .build
  variables:
    WORKSPACE: "@bmi/filters"
    ROOT_DIR: "components/filters"
