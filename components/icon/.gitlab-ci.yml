test_icon:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/icon"
    ROOT_DIR: "components/icon"

build_icon:
  extends: .build
  variables:
    WORKSPACE: "@bmi/icon"
    ROOT_DIR: "components/icon"
