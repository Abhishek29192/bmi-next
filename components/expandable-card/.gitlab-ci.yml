test_expandable-card:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/expandable-card"
    ROOT_DIR: "components/expandable-card"

build_expandable-card:
  extends: .build
  variables:
    WORKSPACE: "@bmi/expandable-card"
    ROOT_DIR: "components/expandable-card"
