test_accordion:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/accordion"
    ROOT_DIR: "components/accordion"

build_accordion:
  extends: .build
  variables:
    WORKSPACE: "@bmi/accordion"
    ROOT_DIR: "components/accordion"
