test_configurator-panel:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/configurator-panel"
    ROOT_DIR: "components/configurator-panel"

build_configurator-panel:
  extends: .build
  variables:
    WORKSPACE: "@bmi/configurator-panel"
    ROOT_DIR: "components/configurator-panel"
