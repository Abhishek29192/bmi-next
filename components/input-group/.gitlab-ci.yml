test_input-group:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/input-group"
    ROOT_DIR: "components/input-group"

build_input-group:
  extends: .build
  variables:
    WORKSPACE: "@bmi/input-group"
    ROOT_DIR: "components/input-group"
