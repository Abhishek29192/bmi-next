test_lead-block:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/lead-block"
    ROOT_DIR: "components/lead-block"

build_lead-block:
  extends: .build
  variables:
    WORKSPACE: "@bmi/lead-block"
    ROOT_DIR: "components/lead-block"
