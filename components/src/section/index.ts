// istanbul ignore file: doesn't hold any logic
import Section, { Props, SectionContext, BackgroundColor } from "./Section";

export type { BackgroundColor, Props as SectionProps };
export { SectionContext };
export default Section;
