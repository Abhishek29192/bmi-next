test_calculator-modal:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/calculator-modal"
    ROOT_DIR: "components/calculator-modal"

build_calculator-modal:
  extends: .build
  variables:
    WORKSPACE: "@bmi/calculator-modal"
    ROOT_DIR: "components/calculator-modal"
