test_google-map:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/google-map"
    ROOT_DIR: "components/google-map"

test_mr_coverage_google-map:
  extends: .test_jest_mr_coverage
  variables:
    WORKSPACE: "@bmi/google-map"
    ROOT_DIR: "components/google-map"

build_google-map:
  extends: .build
  variables:
    WORKSPACE: "@bmi/google-map"
    ROOT_DIR: "components/google-map"
