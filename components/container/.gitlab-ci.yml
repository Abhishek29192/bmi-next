test_container:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/container"
    ROOT_DIR: "components/container"

build_container:
  extends: .build
  variables:
    WORKSPACE: "@bmi/container"
    ROOT_DIR: "components/container"
