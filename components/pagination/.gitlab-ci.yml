test_pagination:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/pagination"
    ROOT_DIR: "components/pagination"

build_pagination:
  extends: .build
  variables:
    WORKSPACE: "@bmi/pagination"
    ROOT_DIR: "components/pagination"
