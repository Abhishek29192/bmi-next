test_quantity-table:
  extends: .test_jest
  variables:
    WORKSPACE: "@bmi/quantity-table"
    ROOT_DIR: "components/quantity-table"

test_mr_coverage_quantity-table:
  extends: .test_jest_mr_coverage
  variables:
    WORKSPACE: "@bmi/quantity-table"
    ROOT_DIR: "components/quantity-table"

build_quantity-table:
  extends: .build
  variables:
    WORKSPACE: "@bmi/quantity-table"
    ROOT_DIR: "components/quantity-table"
