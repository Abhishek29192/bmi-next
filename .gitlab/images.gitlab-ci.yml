.cloud-sdk-image:
  image: gcr.io/google.com/cloudsdktool/cloud-sdk

.cypress-image:
  image: cypress/browsers:node14.17.0-chrome91-ff89

# Mainly used because it includes `git`
.node-full-image:
  image: node:14.18.1

# Can't use official alpine image as we need `autoconf` to be installed
.node-slim-image:
  image: node:14.18.1-slim

.owasp-image:
  image: owasp/zap2docker-stable:latest
